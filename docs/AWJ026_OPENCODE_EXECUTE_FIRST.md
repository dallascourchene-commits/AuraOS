# AWJ-026 — OPENCODE EXECUTE FIRST

**Instruction to OpenCode:** execute this runbook from the owner host. Do not spend a model call rediscovering the architecture before completing the deterministic preflight below.

**Status:** owner-requested engineering work / currentness-bound / nonpromoting. This file does not authorize merge, public release, destructive synchronization, credential disclosure, financial effects, or crossing Gate 10.

## 0. Objective

Make the following path boring and repeatable:

```text
human / ChatGPT / Gemini intent
  -> current Arena resolution
  -> minimum WorkCapsule
  -> exact machine command / AuraOS run request
  -> host currentness + authority + cost admission
  -> C81 / Arena Triadic orchestration
  -> DeepSeek leaf effects through canonical egress
  -> leaf receipts persisted before synthesis
  -> reciprocal synthesis / final rebase
  -> compact RESULT + exact leaf reopen handles
  -> HyperDrive collapse / SuccessorFrame
  -> ChatGPT wake/recommission or Gemini/MCP client continuation
  -> AR/VR projection of the same event graph
```

The immediate success condition is that ChatGPT can author a valid command/work order, AuraOS admits it, DeepSeek workers run through the existing canonical bridge, and the result returns with replay/currentness guarantees. Do not build a second provider plane.

---

# 1. Read the current evidence, not the whole Drive

Current Root Arena at authoring time:

```text
AWJ-001 GEN24
head short: 3aeb8f3db921201f
TP: tp://arena/AURA-DRIVE2/workflow/AWJ-001-LIVE-WORKFLOW-FABRIC?g=24&head=3aeb8f3db921201f
Drive head: 1i_nHZHRhpi_kPqeRAEgAArH0ZC7xKWSOLcXWJ_yve7s
```

**Do not trust this head blindly. Resolve it again before every consequence-bearing step.**

Primary current artifacts:

- Owner Arena-first disposition: Drive `1SnLzRLRDGib2DltXNDKBkgfgI3PWayj6O6b5I8AkyP8`.
- AWJ-023 DeepSeek bridge Gate-10 receipt: Drive `1-ZOcHCDlufwWebLVSLTFKyKLWhOqeQHl`.
- AWJ-023 control-plane/host handoff: Drive `1yI-wW8X8LerZ7zIStoNWTcD1iT78BcALrJmeqdDvML0`.
- AWJ-026 Arena folder: Drive `1pt8xHec6fcbzHifG6kJQOyhx78HO4Yxf`.
- AWJ-026 governing standard: Drive `1UoU6QZxyB-p4XXdciY9Hd7r0LXOyrFs9a2Gh3NBloFY`.
- AWJ-026 human work order: Drive `1RSVfN19rAL0r3MllQVGlqvjLF2qxHd59bRmIJ6boeuw`.
- Current GEN24 owner DeepSeek triad work order: Drive `12_SuF4cIGZNpvGhuCS7qC2hareQZZ3F2Z1lNIhuhxOY`.
- Its role capsules:
  - A+ `1HdserEk_p9hcxZtHEJnascyHqwGeXLvl5NFC7COJqMQ`
  - B- `1H2a2hXfFSMA0SyMnA1h0FeL81_Qae0sc8ZsHvtKnSaA`
  - C0 `1H7apUc7n-C75pDH29u06jT0m33EbHeQESJt15FJCJeA`
- AWJ-025 Gate-10 partial verdict: Drive `1V4M0aUbooyvvRVISNaR1gh8bumP7sn3C`.

Repository surfaces:

- PR #306: Project-006 Drive-command -> canonical DeepSeek D0 egress. Head `e80062d69be35dfa0562cc275b6e08dde8319bc6`.
- PR #307: current Arena/Expert/cost-first/dual-persistence staging. Head `2570602b2b39a925f2ca08fab6672d611a3339bf`.
- AWJ-026 implementation branch: `awj026-arena-deepseek-orchestrator`.
- New provider-neutral scheduler: `core/aura_arena_triads.py`.
- New tests: `tests/test_aura_arena_triads.py`.

## What is already proven — DO NOT RE-SPEND IT

AWJ-023 proved the live host path:

```text
02_SWARM_TO_AURA
-> persisted ingest + hash
-> dedup/currentness
-> WorkCapsule admission
-> Dispatcher V0 governors / lease / fence
-> ACK before provider effect
-> aura_llm_egress.ExternalLLM pinned to DeepSeek
-> RESULT or typed ERROR
-> terminal WAL
-> consumed cursor last
-> 01_AURA_TO_SWARM
```

Proofs include live DeepSeek canaries, replay absorption with zero duplicate provider effect, stale-revision refusal before provider effect, restart absorption, live GEN rebase refusal, a three-worker C81 triad, Dispatcher V0 21/21, and the PR #306 focused suite.

Do not replace this chain with LangGraph, Temporal, Redis, a second DB, a second Drive client, another provider registry, or a new direct DeepSeek HTTP client unless a measured multi-host requirement later invalidates the current design.

---

# 2. Exact installed owner-host machinery

At the last receipt-proven host state:

```text
Windows repo:
C:\Users\pjtra\AuraOS

Aura CLI shim:
C:\Users\pjtra\AppData\Local\Aura\bin\aura.ps1

WSL user:
john_of_wick

Drive ingress only:
/home/john_of_wick/.config/aura-drive/bin/aura_drive_swarm_inbox_v1.py

Drive consumer / orchestration membrane:
/home/john_of_wick/.config/aura-drive/bin/aura_drive_swarm_consumer_v1.py

Currentness helper:
/home/john_of_wick/.config/aura-drive/bin/aura_drive_currentness_v1.py

Envelope dump helper:
/home/john_of_wick/.config/aura-drive/bin/aura_drive_envelope_dump_v1.py

Guarded bus writer:
/home/john_of_wick/.config/aura-drive/bin/aura_drive_bus_writer_v1.py

Dispatcher admission runner:
C:\Users\pjtra\.config\opencode\gdrive\dispatcher\dispatcher_live_admission.mjs

Canonical provider egress:
/mnt/c/Users/pjtra/AuraOS/aura_llm_egress.py

Callback config:
/home/john_of_wick/.config/aura-drive/callback-config-v2.json

Consumer state:
/home/john_of_wick/.config/aura-drive/state/swarm_consumer_v1/consumer_state.json

Current capsule pointer:
/home/john_of_wick/.config/aura-drive/state/swarm_consumer_v1/current_capsule_path.txt

Exactly-once reconcile WAL:
/home/john_of_wick/.config/aura-drive/state/execution_callback_v2/

Outbound intent journal:
/home/john_of_wick/.config/aura-drive/state/outbound_writer_v2/

C81 run receipts:
/home/john_of_wick/.config/aura-drive/state/c81_operator_v1/receipts/
```

Do not restart residents merely to feel clean. Inspect first. Restart only a stopped/broken owner service, and record why.

---

# 3. Phase 0 — deterministic preflight

Run from PowerShell in `C:\Users\pjtra\AuraOS`.

```powershell
git status --short --branch
git rev-parse HEAD

aura status --json
```

Also verify from WSL:

```powershell
wsl systemctl --user status aura-project006.service --no-pager
wsl python3 /home/john_of_wick/.config/aura-drive/bin/aura_drive_currentness_v1.py
```

Read the consumer state without editing it:

```powershell
wsl python3 - <<'PY'
import json
from pathlib import Path
p=Path('/home/john_of_wick/.config/aura-drive/state/swarm_consumer_v1/consumer_state.json')
print(json.dumps(json.loads(p.read_text()), indent=2, sort_keys=True))
PY
```

**Required preflight output:** current Arena generation + digest, active C81 run if any, consumer last cursor/state, service liveness, current capsule generation/digest, and exact repo head/dirty state.

If the current Arena is not GEN24 / `3aeb8f3db921201f...`, immediately rebase all work below to the observed current head. Do not execute a GEN24-bound packet against a later generation.

---

# 4. Phase 1 — finish or classify the DeepSeek work already in flight

The current owner triad (`12_SuF4...`) had an earlier `ORIENTATION_RECEIPT_INVALID` attempt with no effect, followed by corrected role capsules. Do not blindly start a duplicate run.

First:

```powershell
aura status --json
```

If `active_run` is non-null, observe it to terminal/idle and collect its receipt. Do not start another run.

If no active run exists, search the C81 receipts for the work-order/capsule IDs and determine whether a corrected terminal run already completed after the last Drive snapshot. If it did, fold that result. If it did not, and all three corrected capsules are CURRENT, run the existing work order exactly once:

```powershell
aura swarm size 3 --expected-generation <CURRENT_GEN>
aura run --work-order 12_SuF4cIGZNpvGhuCS7qC2hareQZZ3F2Z1lNIhuhxOY --expected-generation <CURRENT_GEN>
```

Poll:

```powershell
aura status --json
```

until lifecycle is IDLE and `active_run` is null. Then restore the ordinary physical target unless current policy says otherwise:

```powershell
aura swarm size 1 --expected-generation <CURRENT_GEN>
```

Collect the C81 run receipt, all role/orientation receipts, exact leaf outputs, final result, provider/model metadata, and any typed child commands. Preserve `UNKNOWN` cost if actual billing is not observable.

### Work this triad is supposed to close

A+:
- rebase AWJ-018 A/B/C to the current head;
- run/prepare only the owner-approved bounded LFM2.5 host benchmark when host effect authority permits;
- run/prepare only the approved dual-persistence canaries;
- fold AWJ-025-R2/current identity evidence;
- never redo AWJ-023 bridge repair.

B-:
- challenge stale currentness, mirror/source collapse, SID/JID/VisitID/WorkerID/certification/lease/effect collapse, self-review, split-brain sync, duplicate effects, model artifact/license/resource risk, accidental continuous autonomy, merge/public/account/monetization widening, provider fallback, and unnecessary inference.

C0:
- verify exact receipts/effect ceilings;
- keep continuous scheduling, PR #307 merge, DNSid-required migration, and public Channel Lab effects held unless a newer explicit owner disposition exists;
- produce PROVEN/PARTIAL/UNOBSERVED/BLOCKED matrix;
- bind successor cognition to U/W/R/B or explicit pending/unassigned plus reopen handles.

---

# 5. Phase 2 — fix AWJ-026 ingress: human document != executable envelope

The existing AWJ-026 human command document in Drive is useful prose, but it is **not** the exact machine shape admitted by PR #306/current host consumer. That is likely why it can sit in the inbox without a command-bound ACK.

The proven machine envelope is `AuraCommandEnvelopeV1-candidate` and currently requires at least:

```json
{
  "schema": "AuraCommandEnvelopeV1-candidate",
  "command_id": "...",
  "idempotency_key": "...",
  "authority_ref": "Drive 1DbZyzpdBIuZUt9jvWogWIesJTHbEqVKGEnmaXO_deO8",
  "queue_state": "AUTHORIZED_FOR_DISPATCH_WHEN_OWNER_BOUND",
  "message_authorized": true,
  "execution_authorized": true,
  "transport": {"type": "CHATGPT"},
  "objective": {
    "text": "bounded D0 objective",
    "requested_effect": "D0",
    "positive_intent": [],
    "negative_intent": [],
    "success_criteria": []
  },
  "constraints": {"workspace_scope": "AURA_DRIVE_ONLY"},
  "requested_capability": {
    "semantic_id_or_alias": "EXISTING_AURA_DEEPSEEK_EXECUTOR"
  },
  "stage_path": ["SOURCE", "BOUND", "AUTHORIZED"],
  "bound_revision": {
    "revision": "AWJ-001@GEN<CURRENT>",
    "digest": "<FULL_CURRENT_HEAD_DIGEST>"
  },
  "target_size": 1
}
```

**Do not change `authority_ref` simply because a newer prose disposition exists.** The last proven callback config expected `Drive 1DbZ...`. Resolve current host config first. If the authority owner intentionally changed, update it through the correct host/governance path and prove the change; do not make the Drive command self-select authority.

Create a new raw `application/json` command file rather than mutating the human work order. Use a fresh command ID/idempotency key and current head.

Then on the host:

```powershell
wsl python3 /home/john_of_wick/.config/aura-drive/bin/aura_drive_swarm_consumer_v1.py allow `
  --command-id <CID> `
  --idempotency-key <KEY> `
  --source-file-id <RAW_JSON_DRIVE_FILE_ID>

wsl python3 /home/john_of_wick/.config/aura-drive/bin/aura_drive_swarm_consumer_v1.py once
```

Read ACK + terminal result from `01_AURA_TO_SWARM` and consumer receipts. Replay the same command once and prove zero duplicate provider effect.

This first AWJ-026 exact-envelope command should be a bounded ingress/capability canary, not the entire project. Its job is to prove `ChatGPT-authored current command -> AuraOS -> DeepSeek -> result` on the current head.

---

# 6. Phase 3 — use existing C81 for real swarm work

Do **not** encode `target_size: 81` into a one-shot provider hook and call that HyperScale.

For multi-agent work, use Aura's native lattice:

```powershell
aura swarm size <N> --expected-generation <CURRENT_GEN>
aura run --work-order <CURRENT_WORK_ORDER_DOC_ID> --expected-generation <CURRENT_GEN>
```

Start with N=3 for an evidence-bearing Construct/Challenge/Verify triad. Expand only after the triad has named independent unresolved cells.

### Default evidence mode: seven-call independent/reciprocal cell

Existing C81 semantics should remain the default for Gate-10/security/currentness work:

```text
A independent leaf
B independent leaf
C independent leaf
A reciprocal BASE_TRIAD
B reciprocal BASE_TRIAD
C reciprocal BASE_TRIAD
FINAL_DIMENSIONAL_REBASE
```

This costs seven provider effects per logical cell but preserves first-pass independence.

### Optional efficient mode: four-call staggered cell

For routine implementation/design where strict first-pass independence is not required:

```text
A Construct
-> B Challenge(A)
-> C Verify(A,B)
-> A Rebase(B,C)
```

Four calls. Mark the evidence class explicitly as `STAGGERED_NONINDEPENDENT`. Never substitute this mode in a test claiming independent verification.

The new branch file `core/aura_arena_triads.py` implements these two modes provider-neutrally and has deterministic tests in `tests/test_aura_arena_triads.py`.

---

# 7. Phase 4 — integrate the new orchestration code correctly

The new scheduler must sit **above** the PR #306 egress seam.

Do not merge a direct DeepSeek HTTP client into production. Do not let the scheduler select API keys, endpoints, provider fallback, leases, fences, currentness, or effect admission.

Recommended integration seam:

```text
Aura Arena scheduler creates LeafCall
-> host adapter derives an admitted child effect
-> currentness check
-> authority/cost admission
-> ACK child effect
-> PR306 / canonical ExternalLLM DeepSeek egress
-> terminal child receipt
-> adapter returns structured LeafResult
-> scheduler persists leaf
-> dependent synthesis becomes eligible
```

Each child effect requires a deterministic unique idempotency key:

```text
<parent-idempotency>/<cell>/<sequence>-<role>
```

If the repository integration requires refactoring PR #306, refactor it **downward**, not sideways:

1. retain its Drive-specific validation wrapper;
2. extract a reusable admitted-DeepSeek-effect function below that wrapper;
3. have both the Drive one-shot hook and Arena leaf adapter call that same canonical function;
4. retain ACK-before-effect, effect admission, redaction, no fallback, and canonical `ExternalLLM`.

Do not create two DeepSeek implementations.

---

# 8. Phase 5 — repair the 6.5 KB cognition-loss boundary

The historical C81 final bus result has been observed truncating around ~6.5 KB. Do not solve this by endlessly raising one text limit.

Implement leaf-first persistence:

1. persist every LEAF result as its own exact artifact before any BASE_TRIAD call;
2. persist every BASE_TRIAD result before FINAL;
3. persist FINAL separately;
4. bus RESULT carries compact status + digests + reopen handles/IDs;
5. full leaf bodies are retrieved only when a verifier/successor actually needs them.

Test a deliberately oversized leaf/result set. The bus summary must stay bounded while all exact bodies remain reopenable and digest-verifiable.

This is the highest-leverage fix for AWJ-025's current evidence gap: do not pay agents again just because a synthesis view truncated their already-produced output.

---

# 9. Phase 6 — DeepSeek efficiency policy

Do not maximize concurrency just because the provider can.

Use the smallest independent frontier. Maintain one byte-stable common WorkCapsule prefix per cell and append role-specific instructions after it. Capture provider usage fields when exposed, including cache-hit/miss data. Stable prefixes allow DeepSeek's automatic prefix cache to amortize repeated source/currentness context.

For every provider effect persist:

- provider/model;
- attempt ID when observed;
- prompt/input/output token observations when exposed;
- cache hit/miss tokens when exposed;
- latency;
- cost or `UNKNOWN`;
- source/currentness bind;
- leaf/result digest;
- invalidators/reopen handles.

No implicit Kimi/Fireworks/OpenRouter fallback.

---

# 10. Phase 7 — Gemini bridge: one AuraOS MCP, not another runtime

Build one remote AuraOS MCP endpoint using **Streamable HTTP** and current MCP semantics. Do not create a separate Gemini truth or worker registry.

Expose narrow tools approximately like:

```text
arena_current
arena_compile_workcapsule
arena_submit_work_order
arena_task_get
arena_task_cancel
arena_get_leaf
arena_debrief
arena_recommission
chatgpt_endpoint_enqueue
```

Long-running `arena_submit_work_order` should return/map to an MCP Tasks handle whose authoritative state is the underlying AuraOS run/C81 receipt. The MCP task record is a projection, not a second execution ledger.

Gemini becomes one authenticated MCP client. Where supported, it can invoke the same Arena tools and can request DeepSeek work indirectly through AuraOS. It never receives raw provider credentials or host authority simply because it can call a tool.

Acceptance:

- Gemini/MCP can ask for current Arena head;
- submit one bounded D0 work order;
- receive task handle;
- poll to terminal;
- reopen leaf receipt;
- no direct DeepSeek key exposure;
- no duplicate runtime state;
- stale work order fails through the same currentness gate as CLI/ChatGPT.

---

# 11. Phase 8 — ChatGPT sleep/wake/recommission endpoint bridge

Only ChatGPT windows use the sleep/wake convention discussed with the owner.

The correct architecture is:

```text
AuraOS remains resident
-> endpoint broker has addressable ChatGPT window/session metadata when the product exposes it
-> work becomes ready
-> broker causes a real new user/tool turn in that window
-> window re-resolves current Arena head
-> hydrates minimum L0/L1 + WorkCapsule
-> works
-> debrief/decommission
-> sleeping again
```

If the platform cannot target/wake the exact prior window, do not simulate it. Create/recommission a successor endpoint and hydrate the prior SuccessorFrame/JID lineage where source-proven.

The 30/60-second interval is only an AuraOS-side endpoint broker latency policy. It is not worker authority. Current owner disposition still holds unattended continuous scheduling; implement and prove the bounded mechanism without silently enabling an always-on paid loop.

Required tests:

- sleeping ChatGPT endpoint cannot self-wake;
- duplicate wake token is idempotent;
- stale capsule on wake reopens/rebases before work;
- closed/full context produces SuccessorFrame and successor endpoint rather than lost work;
- endpoint identity/VisitID/WorkerID distinctions are preserved;
- no timer grants D1/provider authority.

---

# 12. Phase 9 — AR/VR JARVIS control surface

Do not make the visualizer an authority plane.

Project the existing Arena event graph into a spatial scene:

Nodes:
- human/operator;
- Root Arena/Sub-Arenas;
- ChatGPT windows;
- Gemini clients;
- DeepSeek workers;
- WorkCapsules;
- tasks/runs;
- leases/fences;
- source owners/mirrors;
- receipts;
- residuals/reopen triggers.

Edges:
- assigned-to;
- depends-on;
- challenges;
- verifies;
- rebases;
- supersedes;
- source-of;
- mirror-of;
- lease-held-by;
- waiting-on;
- produced-receipt.

Visual state must derive from append-only/event/receipt state. A stale visual node must visibly show stale/superseded rather than disappear as if it never existed.

Human voice/gesture interaction compiles to the exact same typed AuraOS commands as CLI/MCP. Example:

```text
"Open the memory Arena and give this residual to a three-agent challenge triad"
-> intent parse
-> current Arena resolution
-> affected cone
-> command/work-order proposal
-> human confirmation when effect policy requires it
-> ordinary AuraOS execution path
```

No special AR/VR bypass.

---

# 13. Mandatory acceptance matrix

Do not call AWJ-026 ready until these are receipt-backed:

1. current head resolution before effect;
2. stale head refuses before provider;
3. exact machine envelope accepted;
4. human prose document alone does not count as execution;
5. ACK before every admitted provider leaf;
6. replay produces zero duplicate provider effects;
7. restart absorbs already-terminal work;
8. unique child idempotency keys;
9. lease/fence/currentness not caller-selectable;
10. no provider/model/endpoint/key injection;
11. canonical DeepSeek egress reused;
12. no implicit provider fallback;
13. independent mode A/B/C first pass isolated;
14. reciprocal syntheses see the persisted independent leaves;
15. staggered mode explicitly non-independent;
16. call-budget refusal before provider;
17. HyperScale expands independent cells only;
18. every leaf persisted before dependent call;
19. oversized leaf survives bus-summary bound through reopen reference;
20. final result reproducibly binds all leaf digests;
21. preserved dissent survives final collapse;
22. cost remains UNKNOWN where unobserved;
23. cache usage captured when provider exposes it;
24. AWJ-025 existing leaf outputs reused rather than recomputed;
25. dual-drive source-owner/mirror law preserved;
26. SID/JID/VisitID/WorkerID/certification/lease/effect remain distinct;
27. Gemini uses one AuraOS MCP plane;
28. MCP Tasks map to existing AuraOS run state;
29. ChatGPT cannot self-wake;
30. duplicate wake/recommission is idempotent;
31. stale wake triggers rebase;
32. context rollover preserves exact debrief/reopen handles;
33. AR/VR is projection + command compiler only;
34. UI cannot widen authority;
35. held continuous scheduler remains held unless separately authorized;
36. PR #307 remains unmerged unless separately authorized;
37. no public/account/monetization/financial effect;
38. no credential disclosure;
39. automated work stops at Gate 10;
40. final SuccessorFrame names exactly what is proven, partial, open, and what not to re-derive.

---

# 14. Verification commands for the AWJ-026 branch

The provider-neutral scheduler was locally checked with eight deterministic tests before this file was written. Re-run in the actual branch/worktree:

```powershell
python -m pytest -q tests/test_aura_arena_triads.py
python -m py_compile core/aura_arena_triads.py
python -m ruff check core/aura_arena_triads.py tests/test_aura_arena_triads.py --select E9,F63,F7,F82
```

Then run the relevant PR #306/Project-006 suites and Dispatcher V0 suites from their exact integrated source. Do not report a combined green ring if the integration source does not actually contain the tested code.

---

# 15. Branching/integration rule

This AWJ-026 branch was created from PR #307 head, while PR #306 lives on a separate stacked lineage. Do not merge histories blindly.

Use a clean worktree and explicitly choose the integration base after inspecting ancestry. The goal is to reuse the exact #306 egress semantics and #307 Arena/cost-first semantics without accidentally importing unrelated generated artifacts or silently merging held PRs.

A good implementation PR should remain small: provider-neutral triad compiler/scheduler, adapter to the existing admitted egress, leaf receipt store/compact bus result, tests, and focused docs. MCP/AR/VR may be separate follow-on PRs if keeping one proof cone small materially improves review quality.

---

# 16. Return package to Aura Drive / Arena

When finished, write one Gate-10 package into AWJ-026 containing:

- exact repo base/head and changed files;
- current Arena head used;
- host currentness receipt;
- machine command IDs and source Drive IDs;
- ACK/RESULT/ERROR bus IDs;
- C81 run IDs;
- per-worker/leaf receipt IDs and digests;
- test commands and exact pass/fail counts;
- replay/restart/stale evidence;
- provider/model/latency/cache/cost observations;
- all dissent/falsifiers;
- acceptance matrix 1-40;
- code branch/commit/PR references;
- current U/W/R/B coordinates or explicit PENDING/UNASSIGNED;
- held owner decisions;
- exact next smallest residual;
- SuccessorFrame + debrief/decommission.

If a requested effect is not currently authorized or observable, return the typed blocker. Do not fabricate completion.

---

# 17. The central rule

**AuraOS is the persistent nervous system. Models are replaceable workers. Arena is the shared governed workspace. Coordinate cognition prevents recomputation. HyperScale expands only independent unresolved work. HyperDrive collapses verified state. The human sees and manipulates the same state through CLI, ChatGPT, Gemini, or AR/VR — none of those surfaces becomes a second authority plane.**
