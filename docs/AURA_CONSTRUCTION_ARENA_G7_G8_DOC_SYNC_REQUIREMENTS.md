# G7–G8 Documentation Synchronization Requirements

Before merge:

- `README.md` must list the completed G0–G8 Construction Arena chain and local launch command.
- `USER_GUIDE.md` must explain `construction-video-demo`, tours, controls, fallback pack, and authority boundary.
- `.aura/ARCHITECTURE.md` must bind the director as a presentation-only owner above retained G4–G6 owners.
- `.aura/CODEMAP.json`, `.aura/CODEMAP.md`, `topology_map.json`, and `Aura_Memory/live_topology_ast.json` must be regenerated from the final source tree.
