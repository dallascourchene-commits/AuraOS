# BugHound Hard-Case Precision Router — O9

Status: D0 / local benchmark routing only / nonpromoting / no live-target authority.

## Exact two foreign rebase parents
1. Generation-Aware Evidence Invalidation / Exact-Head Reproof V1 — Drive `17eYEphv5vvtkVSZzVtLcrOt3zTGK_R_pVVxqPZfFkQE`.
2. Ambiguity Escalation / Precision Budget Controller V1 — Drive `1s0wStpFLBiwPd_tkjnIrY3-b7mRK9JF7EF9c6lmxhSI`.

O9 is stacked on O8 real-corpus hydration. The router first invalidates stale source/oracle generations, then selects the minimum sufficient local tier:

T1 currentness → T2 structural → T3 path-sensitive → T4 interprocedural/dataflow → T5 stateful/symbolic → T6 local dynamic/fuzz → T7 L4 vulnerable/fixed counterfactual.

Incomplete dependency coverage widens local validation but does not mint owner, external, testing, network, credential, submission or payment authority.

Research pressure from FuzzingBrain V2 and SAEFUZZ is used as method pressure for layered suspicious-point/static+dynamic routing and state-aware analysis. Their published results are not transferred to BugHound.

## Keeper laws

`StaleGreenEvidence != CurrentProof`.
`GeneratedProviderMovement != SemanticMovement`.
`Ambiguity != MandatoryExpensiveAnalysis`.
`UnknownDependencyCompleteness => WiderLocalValidationDebt`.
`LocalWiderValidation != OwnerAuthority`.
`StaticSuspicion != DynamicReproduction`.
`DynamicReproduction != CounterfactualSpecificity`.
`DifficultyVector != ExploitabilityProbability`.
`ToolRoute != TestingAuthority`.
`BenchmarkOptimization != LiveTargetAuthorization`.
