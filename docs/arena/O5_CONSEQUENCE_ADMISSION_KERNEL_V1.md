# O5 Consequence Admission Kernel V1

Status: **D0 / nonpromoting / Gate10 false**.

## Exact two fresh foreign rebase parents

1. **Advancement Capsule Compiler V1.1 O2 — Omega-8 Hard-Invalid Dominance** (Drive `1MbeD89PvynYlsLXwvakjkkAc3yQKy9aTR4LMwPwX24s`). It reproduced the full `3^8 = 6,561` Omega-8 space, found that 19 of 27 prior keeper states admitted at least one hard-invalid axis, and showed 729 13D routing/context witnesses that could not lawfully repair that failure.
2. **Event-Sourced Arena Ledger V1 O1** (Drive `1gTxaBMFYSduQgRgccweSRdtEgJKqnTWoaPuFFklYKNE`). It contributes append-only hash-chained transitions, deterministic projection, currentness/semantic separation, and smallest dependency-cutset replay.

J59/V07 is inherited control, not a third parent: succession transfers reproof duties rather than beliefs; derived memory may route a proof but must exit to current source/owner/evidence or fail closed.

## Objective

Make consequence admission and causal replay commute across current Arena projects:

- any hard-invalid Omega-8 axis dominates to HOLD;
- the five trailing Y13 routing/context coordinates cannot repair a failing first-eight state;
- required evidence axes remain noncompensatory;
- READY requires a current source-exit and no declared dependency debt;
- READY remains nonauthorizing;
- every READY/HOLD is appended to a tamper-evident event chain;
- an invalidator wakes only projects indexed by the changed dependency and never auto-readmits them;
- successor handoff carries re-adjudication obligations and source-exit locators, never inherited truth or authority.

## Initial project fixtures

`BUGHOUND_O12`, `AWJ032_R2`, `AURAOS_796`, `COUNCIL_V3`, and `O4_FRONTIER`.

## Proof

- 22 focused tests in each of three freshly recreated stdlib-only venvs = **66/66 PASS**.
- 300,000 randomized mixed-policy Y13 decisions against an independently spelled policy oracle = **0 mismatches**, **0 authority escapes**, **0 hard-invalid admissions**, **0 routing-repair escapes**.
- Exact parent defect surface: `3 hard-invalid optional axes × 3^5 trailing coordinates = 729` witnesses per environment; **0 escapes**.
- Independent verifier: full 6,561 Omega-8 census + 729 exact defect witnesses + 50,000 disjoint mixed-policy cases = **57,290 checks / 0 mismatches**; exactly **8 lawful READY** Omega-8 states.
- 1,000-stream portfolio replay: invalidating `semantic_root` woke exactly **200/1,000 AURAOS_796-family streams**, with the append-only chain valid before and after invalidation.
- Repository-layout transport rerun: **22/22 PASS**.

Local semantic source SHA-256: `5fbfeb8073ecccc58b091fdfda3ce611811f529da57166518efe63d5483930b4`.
Repository-layout test SHA-256: `cd25546fff6dfaccbfe641688eaee99d9b5b8514311216b9b31fa26b4fb1dfd7`.

## Research pressure

Recent event-sourcing work (ESAA; *The Log is the Agent*) supports append-only state mutation, deterministic replay, and cheap forking. PBT-Bench supports invariant-first evaluation rather than relying on broad random testing. Practitioner discussions repeatedly distinguish an action log from an audit trail that can prove which policy/version/permission chain was active.

These sources are design/falsification pressure only; no external empirical result is transferred as Aura performance credit.

## Keeper laws

`AnyHardInvalidOnOmega8 => HOLD`.

`Routing5 != ConsequenceRepair`.

`AggregateEvidence != MissingNamedSliceEvidence`.

`AdmissionREADY != TruthAuthority != EffectAuthority != Gate10`.

`Invalidator(keys) => ReopenSmallestIndexedCutset(keys)`.

`Invalidation != AutoReadmission`.

`SuccessionTransfersReproofDutiesNotBeliefs`.

`DerivedMemoryMayRouteProofButMayNotTerminateProof`.

## Claim ceiling

No canonical AuraOS absorption, merge/deploy, provider/model execution, owner-host effect, production durability, generalized/open-world truth, public/financial effect, private/native transformer KV access, semantic K27 authority, or Gate10 is claimed.
