# AWJ-001 GEN25 Descendant Rebase Barrier V1

Status: D0 / HS1 / nonauthorizing / exact two-foreign-parent convergence.

## Creation Contract
Mission: preserve deterministic currentness across AWJ root promotion without allowing stale descendant state to execute.
Purpose: make GEN24-bound work fail visibly and cheaply after GEN25 promotion, while keeping root authority separate from descendant/effect authority.
Objective: require WorkCapsules, commands, temporal owners, host observations, leases and fences to bind the current AWJ root generation/head before consequential use.
Success: exact two-parent ancestry; focused adversarials; complete 2^8 Different-J; hosted reproduction of GEN24 REBASE_REQUIRED and GEN25 CURRENT_CANDIDATE; full negative effect ceiling.
Constraints: no main/public/financial/destructive/credential effect; no inherited authority; no semantic K27 authority; no private/native transformer KV claim.

## Exactly two foreign hosted-green parents
1. PR #559 semantic exact `26201c63c3531bbf631ef34803c6f01ccd7499d3`; workflow `Aura WorkCapsule Temporal Host Observation Admission`; run `33355149887` SUCCESS. Law: temporal/local closure and host observation completion do not imply host/effect readiness.
2. PR #556 semantic exact `5fae4070f82a9b5882ae0f63877359bf6e5a9a2b`; workflow `Aura WorkCapsule Raw-Owner Derived POST Closure Transition`; run `33354561517` SUCCESS. Law: the POST lifecycle must be causally derived from distinct evidence phases; phase-root collapse fails closed.

True two-parent convergence: `cf9f66a7854b34111566c6f532ebf45af9a82343`.

## Current AWJ root input
Typed promotion receipt Drive `1yIwhSYDQbYLQRVSLIgGerARriDOyYof4uiXh2---chY` establishes AWJ-001 GEN25 / `d91e0a39358901c5` at its promotion cut. Future uses still re-resolve currentness.

## Eight-gate membrane
1. exact typed root receipt;
2. descendant compiled root generation;
3. descendant compiled root head;
4. temporal-owner generation;
5. host-observation-owner generation;
6. command root-currentness binding;
7. lease/fence root binding;
8. claim ceiling.

The positive state is `CURRENT_CANDIDATE_NONAUTHORIZING`, never effect readiness. Any stale descendant gate yields a typed HOLD/REBASE disposition. GEN24-bound descendants under GEN25 therefore cannot continue by inertia.

## Crystalline / Triadic / Creation / HyperScale
W0: authenticated root + two exact foreign owners.
W1: GEN24 stale descendant -> typed rebase -> GEN25 current candidate roundtrip.
W2: generation/head/temporal/host/command/lease/authority substitutions.
W3: exact-host contradiction -> smallest repair only.
W4: root currentness, temporal provenance, host observation rank, command binding, lease/fence, semantics and authority remain separate leaves.
W5: true two-parent diamond convergence.
W6-W8: unearned; do not ritualize unused topology.

Triadic: host-currentness thesis + causal-phase thesis -> root-promotion stale-descendant challenge -> version-fenced descendant rebase barrier.
Creation: bind exact parents -> bind current root -> model descendant axes -> challenge substitutions -> derive typed holds -> exhaustive Different-J -> hosted proof -> persistent collapse -> effect gate.
HyperScale: HS1, finite eight-bit consequence cone. No swarm is useful while the complete classifier is exhaustible locally/hosted.

## Core laws
`RootPromotionInvalidatesStaleDescendantCurrentness`.
`GEN24BoundDescendantUnderGEN25 => RebaseRequiredBeforeEffect`.
`HostObservationCurrentnessDependsOnTemporalOwnerGeneration`.
`POST_CLOSED != HOST_OBSERVATION_COMPLETE != HOST_EFFECT_READY`.
`MatchingGEN25Descendant => CurrentCandidateOnly`.
`RootAuthority != InheritedDescendantAuthority`.
`K27Placement != SemanticIdentity != Currentness != Authority`.
`CoordinateMemory != MODEL_PREFIX_KV`.
