# AWJ032 / GLM-5.3 — G5 v2 G4-currentness + alias-stable recompute admission

Status: D0 / HS1 / NONPROMOTING / ARENA CANDIDATE
Date: 2026-08-31

## Objective

Repair G5 after two independently discovered generation defects:

1. canonical G4 advanced from caller-structural `REVALIDATED_UNCHANGED` to G4-v2 `STRUCTURAL_MATCH_OWNER_AUTH_REQUIRED`;
2. PR #759 proved that K27/scheme route projection rotation can make raw retrieval resources change without semantic source progress.

Therefore:

`LegacyG4StructuralEquality != OwnerCurrentness != ReuseAuthority`.

`RouteProjectionChanged != SemanticRetrievalAxisChanged`.

`G4HoldRecompute + AliasStableProgress + SourceVersionState != LawfulBoundedRecomputeAttemptUntil CurrentG4V2Shape + SemanticProgress + ExactVersionEdge + FutureReadCurrentness Commute`.

## Exact current substrate

G4-v2 owner generation:
- head `025d619d24d95dd6acc29981b1bd61bce92e25a3`;
- source blob `3e5686c3fe635f59e85768e1270be98175983e03`;
- test blob `f0b0b6e0c62588e6325452b57bbd8bae246de6a0`.

G4-v2 deliberately grants no reuse on structural equality. `STRUCTURAL_MATCH_OWNER_AUTH_REQUIRED` is a HOLD boundary. Actual axis drift remains `HOLD_RECOMPUTE_G3`.

## Exactly two foreign semantic parents

### Parent A — PR #759 / K27 alias-aware retrieval progress

Semantic head `658b3bc651ee39454f6b94039d26ff76d48f73d8`.
Source blob `1abd821beb2a8a9a96b5ac2f0956195b20a321c7`.
Test blob `ddc88a73f49d6a09d67b388cf5c4958317e10ae2`.
Proof-only head `cf6b07e5c498d7c429e6679a8ba5cec5e1e46ca6`.
Hosted run/job `33436588718 / 99634405807`, SUCCESS.

Reusable law: same semantic source under another route/scheme cannot reset no-progress debt. Missing alias resolution is a HOLD. Alias projection may restrict progress but cannot grant truth/currentness/authority.

### Parent B — PR #755 / EKI-4 version-transition envelope

Head `162fdb9c69f288090845453a67d1f41da28e8a53`.
Source/test blobs `7ac33764ee238098a2887af96344ed642565ac48` / `10c83c2432636908b237fe171eebd0714a10788f`.
Hosted run/job `33435683382 / 99631408076`, SUCCESS.

Reusable law: explicit predecessor→successor representation does not pay future read-currentness debt.

## Typed outcomes

- `HOLD_G4_OWNER_CURRENTNESS_REQUIRED`
- `HOLD_ALIAS_RESOLUTION_REQUIRED`
- `HOLD_RETRIEVAL_AXIS_CHANGE_REQUIRED`
- `COLLAPSE_RECOMPUTE_CONE`
- `HOLD_VERSION_TRANSITION_REQUIRED`
- `HOLD_SOURCE_READ_CURRENTNESS_REQUIRED`
- `ADMIT_BOUNDED_G3_RECOMPUTE_ATTEMPT`

The final state is control-plane admission only. It is not recompute execution, model/provider execution, transfer execution, routing mutation, physical I/O proof, source truth/currentness minting, or Gate-10.

## W3 repairs

### W3-A — stale G4 success shape

Old G5-v1 treated `REVALIDATED_UNCHANGED` as `NOT_APPLICABLE_RECOMPUTE`. That shape no longer exists in canonical G4-v2. G5-v2 rejects it and preserves owner-currentness debt.

### W3-B — route-token progress laundering

Raw #754 `ALLOW_CHANGED_AXIS` can be triggered by a changed resource token. #759 quotients K27/scheme route views through stable source SID and requires alias-owner resolution for same-source route changes. G5-v2 consumes only the alias-aware projection.

## External Different-J pressure

External sources are methodology/falsification pressure only and grant no Aura authority.

1. SpecPrefetch — arXiv:2607.24787. Speculative expert prediction is used for asynchronous transfer while the native router remains authoritative; feasibility depends on cache/bandwidth state.
2. SpecMD — arXiv:2602.03921. Expert caching behavior and policy performance depend on access patterns and hardware; temporal locality assumptions can fail.
3. Current GLM-5.3-Flash community benchmark (LocalLLaMA, 2026-08-28). Warm shape-keyed graph cache, CPU-MoE offload, engine implementation and hardware materially alter observed throughput.
4. `SCHOLAR_DIRECT_GAP`: direct task-specific Scholar-native discovery yielded no stronger stable record for this exact relation in this pass.

### External-world coordinate memory

Non-authoritative deterministic reopen mapping only. Scheme label: `AURA-EXT-B3MOD27-XYZ-v1-local-reopen`. Coordinates are derived from SHA-256(locator), first three 32-bit limbs mod 27. They are not canonical semantic K27 owner allocations.

- SpecPrefetch locator digest `05fa720414a31c670d7eef9f7bf1707bf7259c3419fb76248225a6e832064cca` -> `(19,26,12)`.
- SpecMD locator digest `466313fa47d3235d8b5441ce9342aaf6f2b88dbe49a60ba83c32eca41eb835d9` -> `(0,8,5)`.
- GLM-5.3 TensorSharp/llama.cpp Reddit locator digest `5ceefcc4b3486a573ca145fdb12399dc30f1a4052e8b043008958a68c8673d69` -> `(10,6,15)`.
- Scholar-gap record digest `4d62c222b56be7ae0f4d64c6aa68eccdb4ff43e08e692a09d4d1bbdc35f5e29b` -> `(16,12,0)`.

`ExternalCoordinate != EvidenceTruth != Currentness != Authority != PhysicalPlacement`.

`CoordinateMemory != MODEL_PREFIX_KV`.

## Triadic Process

Thesis: a G4 drift receipt identifies a recompute need.
Counterplane A: raw retry activity can be fake progress under K27/scheme alias rotation.
Counterplane B: version representation can change while read-currentness debt remains unpaid.
Synthesis: only current G4-v2 drift plus alias-stable progress, and for source drift an exact version edge plus current read witness, can produce a bounded recompute-attempt candidate.

## Creation Process

1. freeze exact current G4-v2 source/test generation;
2. freeze exact terminal-green #759 and #755 evidence;
3. quotient old G5-v1 stale semantics;
4. factor structural-currentness / semantic-progress / source-version / read-currentness axes;
5. implement independent tree and ordered-table classifiers;
6. attack legacy G4 success, route-alias laundering, no-progress loops, version/currentness cross-casts and authority widening;
7. exhaust the bounded control lattice;
8. host exact proof;
9. persist only earned proof coordinates;
10. recurse only from two fresh consequence-distinct other-Agent artifacts.

## Eight crystalline lenses / HyperScale

- W0 provenance: exact heads/blobs/runs/jobs.
- W1 ordered flow: G4-v2 state -> alias-stable progress -> version edge -> read-currentness -> candidate/HOLD.
- W2 substitution: legacy G4 state, route alias, SID, generation, version edge, read witness, authority fields.
- W3 contradiction: old structural success and route-token progress both falsified.
- W4 factorization: planning/currentness/progress/version/evidence/execution/effect remain independent leaves.
- W5 synthesis: #759 × #755 over current G4-v2 substrate.
- W6 quotient: route aliases collapse to stable semantic source identity for progress debt.
- W7 temporal invalidators: G4 axes, provider/evidence generations, source generation and read-currentness remain identity-bearing.
- W8 effect plane: unearned and therefore inactive.

HS1 is sufficient: finite exhaustive state proof is stronger than synthetic worker fanout for this membrane.

## Claim ceiling

No GLM/model/provider/retrieval execution; no network/provider effect; no transfer effect; no physical NVMe observation; no native-router mutation; no source-currentness minting; no semantic truth; no causal speedup/quality claim; no semantic K27 authority; no native/private transformer KV access; no G2/Gate-10; no merge/deploy/spend/public/financial/human effect.

## Supersession

This objective supersedes the semantic closure path of PR #766. PR #766 remains useful audit provenance for the first G5 formulation but must not receive closure credit against the superseded G4-v1 success shape or raw #754 progress projection.
