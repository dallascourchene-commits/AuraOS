# AWJ032 G3 Hosted Proof Expectations

The dedicated workflow `Aura GLM53 G3 Abstention Safe Transfer Admission` is the terminal evidence boundary for the current G3 semantic generation. The inherited `Aura GLM53 G2 W3 Rebase Proof` must also be descendant-safe and green rather than requiring a lawful child to remain byte-identical to a historical parent.

A repaired-green result must establish all of the following:

1. `e74bcbaa55f5d4f8b09427293a5638089d5dface` is an ancestor and has exactly green G2 parent `44831fd...` plus green abstention parent `d0f71761...`.
2. The exact G2 parent source blob and exact abstention-parent source blob are authenticated.
3. Parent hosted runs are still exact-green.
4. The composed G1 source contains both abstention canonicalization and the post-read pager-result source/expert-set proof.
5. Existing G1 and G2 adversarial suites still pass.
6. G3 adversarials pass, including empty forecast-set closure, policy identity, exact native demand loading, dishonest pager-result rejection, and rejection of numerical zero as a substitute for NOT_APPLICABLE reuse feasibility.
7. Empty prediction produces:
   - zero speculative logical bytes;
   - zero admitted bytes/time/margin;
   - `cold_required_reuse_for_window is None`;
   - `cold_required_reuse_disposition == NOT_APPLICABLE_ZERO_LOGICAL_BYTES`.
8. A positive-byte prediction still produces a numerical W4 reuse value with disposition `APPLICABLE_POSITIVE_LOGICAL_BYTES`.
9. Empty transfer planning does not create physical-I/O evidence, G2 admission, transfer authority, routing mutation, semantic K27 authority, native/private transformer KV access, or any external effect.
10. The inherited G2-W3 proof authenticates the exact historical G1-W3 artifact at its pinned commit, proves the live descendant retains the pager-result scar, and reexecutes the current G1/G2 contracts. It must not require the live descendant blob to equal the historical blob.

Historical note: run `33427201378`, job `99603555875`, is a valid SUCCESS for the superseded generation that encoded zero-byte reuse as numerical `0.0`; it cannot close the repaired generation.

Any contradiction reopens only the smallest invalidated cone.
