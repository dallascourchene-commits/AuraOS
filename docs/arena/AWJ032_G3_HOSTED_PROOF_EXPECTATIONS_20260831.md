# AWJ032 G3 Hosted Proof Expectations

The dedicated workflow `Aura GLM53 G3 Abstention Safe Transfer Admission` is the terminal evidence boundary for this objective.

A green result must establish all of the following on one child head:

1. `e74bcbaa55f5d4f8b09427293a5638089d5dface` is an ancestor and has exactly the green G2 parent `44831fd...` plus the green abstention parent `d0f71761...`.
2. The exact G2 parent source blob and exact abstention-parent source blob are authenticated.
3. Parent hosted runs are still exact-green.
4. The composed G1 source contains both abstention canonicalization and the post-read pager-result source/expert-set proof.
5. Existing G1 and G2 adversarial suites still pass.
6. G3 adversarials pass, including empty forecast-set closure, policy identity, exact native demand loading, and dishonest pager-result rejection.
7. Empty prediction produces zero speculative logical bytes, zero admitted bytes/time/margin, and `cold_required_reuse_for_window == 0.0`.
8. Zero transfer planning does not create physical-I/O evidence, G2 admission, transfer authority, routing mutation, semantic K27 authority, native/private transformer KV access, or any external effect.

Any contradiction reopens only the smallest invalidated cone.
