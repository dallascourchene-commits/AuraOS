# AWJ032 GLM-5.3 G3 — Abstention-Safe Transfer Admission

Status: D0 / HS1 / NONPROMOTING / W3 REOPENED UNTIL REPAIRED EXACT-HOST SUCCESS

## Exactly two other-Agent artifacts

1. PR #722, exact green semantic head `44831fd5454d08a97bd0b172337cf4c48a339dfe`: cost-aware speculative-transfer admission. It owns typed latency value/cost, calibration identity, bounded logical-byte/time/energy gates, and `TransferPlanEligibility != TransferEffectAuthority != G2Admission`.
2. PR #723, exact green head `d0f71761b1d8692cc3797d4d8574b4082f404344`: safe predictor abstention. It proves `PrefetchPrediction(())` is lawful while the native route remains nonempty and authoritative, and that abstention demand-loads the exact native-selected experts without inventing physical-I/O savings.

True two-parent semantic convergence: `e74bcbaa55f5d4f8b09427293a5638089d5dface`.

## Objective

Close the cross-artifact seam:

`SafePredictionAbstention + CostAwareTransferAdmission != LawfulEmptyTransferPlanUntil EmptyForecastSet + ZeroColdBytes + ReuseFeasibilityNotApplicable + NativeDemandContinuity + PhysicalIOUnknown Commute`.

PR #723 makes the empty prediction lawful. PR #722 inherits W4 `required_reuse()`, whose domain requires positive logical bytes. The empty case must therefore bypass the ratio and preserve the mathematical absence explicitly rather than fabricate a numerical reuse requirement.

## W3 collision and supersession

The first G3 generation repaired the crash by mapping zero predicted bytes to `cold_required_reuse_for_window == 0.0`. Hosted run `33427201378`, job `99603555875`, succeeded for that generation.

A later collision scan surfaced PR #732, an independently derived source-level falsifier for the same seam. Its useful distinct pressure is:

`ZeroPredictedBytes => RequiredReuseNotApplicable`.
`RequiredReuseNotApplicable != ZeroReuseRequirement`.

The objection is valid from W4's own positive-byte domain, so G3 reopened despite the previous green proof. PR #732 is retained as falsifier/support lineage only: it is not a third parent, not a successor artifact, and duplicate semantic mass gets zero sibling credit.

The earlier green run remains valid historical proof of the superseded numeric-zero generation only.

## Current repair

- preserve #722's post-read pager result source/expert-set W3 guard;
- preserve #723's speculative-prediction canonicalizer that allows the empty predictor set while keeping native-route expert sets nonempty;
- for zero predicted logical bytes:
  - `cold_required_reuse_for_window = None`;
  - `cold_required_reuse_disposition = NOT_APPLICABLE_ZERO_LOGICAL_BYTES`;
- for positive predicted bytes, call the exact W4 `required_reuse()` equation and emit `APPLICABLE_POSITIVE_LOGICAL_BYTES`;
- reject forged numerical `0.0` as an impersonation of NOT_APPLICABLE;
- require the forecast set to equal the prediction set, therefore empty under abstention;
- preserve policy layer/binding/currentness checks even when no experts are predicted;
- keep admitted experts, logical bytes, transfer time and expected latency margin at ordinary additive zero values;
- keep physical I/O unattested/UNKNOWN and transfer/G2/effect authority false;
- prove native misses are still demand-loaded exactly and still subject to pager-result source proof.

## Descendant-safe historical proof rule

The inherited G2-W3 proof previously required the live downstream G1 blob to remain byte-identical to a historical G1-W3 blob. That is too strong for a lawful semantic descendant.

The repaired child workflow instead:
- authenticates the historical G1-W3 blob exactly at its pinned commit;
- proves the live descendant still contains the pager-result binding/expert-set guard;
- reexecutes the current G1 contract;
- reexecutes the positive-byte G2 path.

`HistoricalExactArtifact != RequiredLiveDescendantByteEquality`.

## Laws

`PredictionAbstention != RoutingFailure`.
`EmptyPrediction => ZeroSpeculativeTransferPlan`.
`ZeroSpeculativeTransferPlan != EmptyNativeExecutionPlan`.
`ZeroPredictedBytes => ReuseFeasibilityNotApplicable`.
`ReuseNotApplicable != ZeroReuseRequirement`.
`NumericalZeroCannotImpersonateNotApplicable`.
`ForecastSetMustEqualPredictionSet`.
`AbstentionDoesNotBypassPagerResultSourceProof`.
`TransferPlanEligibility != TransferEffectAuthority != G2Admission`.
`K27Coordinate != TransformerKVCache != RoutingAuthority`.

## External Different-J pressure

- Speculative-expert and confidence-aware MoE prefetch research supports making prediction optional/adaptive rather than execution-authoritative.
- SPICE (arXiv:2608.21240) reinforces confidence-aware staging, while its approximation-on-miss path remains outside Aura's exact-demand recovery cone.
- arXiv:2603.19289 independently supports speculative expert transfer overlap as a separate optimization plane.
- Current LocalLLaMA reports show both gains and regressions from expert caching/prefetch depending on workload and reuse, reinforcing abstention as a valid policy outcome rather than forced traffic.
- Direct task-specific Google Scholar-native discovery returned no stable result in this pass: `SCHOLAR_DIRECT_GAP`.

External sources are falsification/methodology pressure only and grant no Aura authority.

## K27 / persistent-coordinate allocation

Existing expert-prefetch and cache-pressure coordinates are reused rather than duplicated. Relation coordinate:

`K27:AWJ032:G3:ABSTENTION_SAFE_TRANSFER_ADMISSION -> {parents:#722,#723, convergence:e74bcbaa..., state:W3_REOPENED, reopen_reason:PR732_ZERO_BYTE_REUSE_DOMAIN, reopen:[predictor_generation, calibration_generation, policy_generation, binding_digest, G1/G2 semantic generation, proof_generation]}`.

This coordinate is retrieval/currentness/reopen metadata only. It is not transformer KV, semantic truth, routing authority, pager authority, or effect authority.

## Crystalline / Triadic / Creation / HyperScale

W0: exact green #722 + #723 anchors.
W1: predictor abstains -> empty forecast set -> empty transfer plan -> native route remains independently executable/demand-bound.
W2: forecast injection, policy layer/binding substitution, dishonest pager-result source, numeric-zero/N-A substitution, authority/physical-I/O widening.
W3: first `required_reuse(0)` contradiction; second independent `0.0 != NOT_APPLICABLE` collision correction.
W4: prediction, calibration, plan, native route, pager result, physical I/O, execution and effect remain independent leaves.
W5: true two-parent Diamond at `e74bcbaa...`.
W6: collision semantics quotient PR #732 to falsifier lineage without third-parent or successor credit.
W7: predictor/calibration/policy/source/proof generations remain identity-bearing reopen triggers.
W8: transfer/effect/G2/Gate-10 authority remains unearned.

HS1 remains sufficient; no swarm expansion is justified by this single deterministic relation cone.

## Proof state

Superseded generation: run `33427201378`, job `99603555875`, SUCCESS, historical only.

Repaired NOT_APPLICABLE generation: exact-host success required before re-closing G3. The dedicated workflow plus descendant-safe inherited G2-W3 proof must both validate the current semantic generation.

## Claim ceiling

No GLM/provider/model execution, checkpoint download, host transfer, physical NVMe measurement, output-quality claim, G2 admission, native/private transformer KV access, semantic K27 authority, Gate-10, merge/deploy/spend, or public/financial/human effect is granted.
