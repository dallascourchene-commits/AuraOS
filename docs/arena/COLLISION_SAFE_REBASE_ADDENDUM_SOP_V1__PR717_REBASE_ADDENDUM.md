# PR #717 Rebase Addendum to Collision-Safe SOP V1

Status: D0 / nonpromoting / append-only cognition update.

## Collision observed

PR #717 (`Triadic addendum: bind Q22 directed provenance into O67 attachment`) predates PR #718 and independently owns the concrete Q22/O67 collision-addendum application. PR #718 therefore does **not** claim ownership of that domain relation or priority for the general observation that collision need not mean discard.

PR #717 contributes these reusable laws:

- `Collision != Discard`.
- `SharedConclusion => PreserveCanonicalOwner`.
- `LocalDelta => TypedAddendum`.
- `TypedAddendum + OtherAgentArtifact => NewObjectiveCandidate`.
- `SameConclusionMayIncreaseSupportWithoutMintingDuplicateConsequenceMass`.

## Rebased role of PR #718

PR #718 remains useful only for its non-duplicate residual:

1. a generic typed collision-assessment contract reusable outside Q22/O67;
2. deterministic dispositions for stale-owner, duplicate-lineage, overlap+residual and orthogonal cases;
3. explicit `sibling_credit_earned=false`, semantic-authority=false and effect-authority=false ceilings at assessment time;
4. an Aura-wide operational sequence for owner-currentness resolution, overlap quotient, residual revalidation, persistent negative knowledge and minimum-cone reopen;
5. the AirLLM/GLM-5.3 Gemini collision as an independent conformance case.

Accordingly:

`PR717ConcreteCollisionOwner != PR718GenericCollisionDecisionContract`.

`SameConclusionFromPR717AndPR718 => MoreDerivationSupport, NotTwoSemanticConsequences`.

`GenericSOPMayConsumeConcreteLawWithoutTakingConcreteDomainOwnership`.

## Gemini residual after this rebase

The Gemini thread still supplies one useful independent analytical vector for AWJ032:

`NominalExpertPayloadReduction = 1 - SelectedExpertPayloadBytes / CandidateExpertPayloadBytes`.

Equal-size top-2-of-8 expert payloads imply 75% nominal expert-payload reduction, but:

`NominalTopKPayloadReduction != ObservedPhysicalIOAvoidance != LatencyHidden != EndToEndPerformance`.

PR #408 remains the physical-I/O accounting owner. PR #311 remains the AirLLM hard-false security owner. AWJ032 pager lineage remains the GLM paging owner.

## Successor-credit rule

This rebase addendum itself is not a fresh sibling merely because it was written later. Reusing PR #717's conclusion earns no duplicate semantic mass. Any successor must depend on a consequence-distinct residual that survives current-owner revalidation and independent proof.
