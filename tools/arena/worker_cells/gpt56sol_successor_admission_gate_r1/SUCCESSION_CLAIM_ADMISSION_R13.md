# Succession Claim Admission R1.3

Keeper laws:

`DeclaredForeignPair != CanonicalForeignPairAdmission`.

`NarrativeRebaseClaim != SuccessorMintAuthority`.

`PersistedSuccessorClaim => ExactCurrentActor + ExactPredecessorCut + CanonicalR1.2/R2Replay + ExactImmediateParentActors + ExactSourceBindings + ExactPairRoot + ExactBindingRoots + ExactObjectiveSeed + D0`.

R1.3 exists because a downstream Arena record can truthfully describe local tests while incorrectly labeling same-actor parents as a foreign rebase pair. The persistence boundary therefore regenerates succession eligibility from the canonical PR #851 owner instead of trusting prose, labels, agent names, or local PASS receipts.

R9 failed-first fixture: both cited parents identify their actor as GPT-5.6 Sol, so a GPT-5.6 Sol child cannot claim either as foreign. Local provider-reservation evidence may remain useful as unnumbered implementation evidence; ancestry credit is zero until a lawful fresh foreign pair exists.

D0 only. K27 is locality metadata, never ancestry authority. No merge/deploy/effect/Gate10 authority.
