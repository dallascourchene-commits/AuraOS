# AGENT_09 — Recomputed Evidence Cost Admission

D0 worker cell. It removes caller-supplied proof booleans from the benchmark-efficiency path.

Exact foreign rebase parents:
1. AGENT_06 / PR #827 review consequence: `atomic_semantics_preserved`, source currentness, and envelope claims cannot be trusted as self-asserted prerequisites; parent proof must be authenticated or recomputed from canonical evidence.
2. AGENT_01 / PR #825 current runtime consequence: cumulative speculative-energy state must use one exact canonical representation across the run; long-run float accumulation can reject an exact boundary.

Keeper law:

`EfficiencyCredit => RecomputedSourceIdentity ∧ RecomputedFusedTrace ∧ WorkloadIntegrity ∧ TransferTraceBinding ∧ ExactCumulativeBytes→DecimalCost ∧ ExactParentRootComposition ∧ D0`.

No `source_current` or `atomic_semantics_preserved` input boolean exists. Source currentness is equality of exact heads plus runtime-byte SHA-256. Fused-event validity is reconstructed from unique token×layer slots and native expert tuples. Workload ranking fails on cross-category exact-prefix collisions. Transfers must bind to real fused-event IDs. Cumulative cost is derived once from integer byte totals with Decimal at the cost boundary.

This does not execute GLM-5.3, claim physical power/throughput, alter native routing, authorize a merge/deploy, or create Gate10 authority.

## Currentness ceiling

`current_head` is raw evidence supplied to this D0 compiler and is cross-checked against `source_head`; runtime bytes are independently SHA-256 recomputed. This removes naked validity booleans but is **not provider-authenticated currentness**. A live GitHub/owner-host observation remains an upstream proof obligation before physical or promotion claims. Child source/trace/workload/cost receipts are nonauthorizing; only the composite surface can issue D0 efficiency credit.
