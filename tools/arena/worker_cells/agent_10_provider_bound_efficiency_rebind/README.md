# AGENT_10 — Provider-Bound Efficiency Rebind

D0 succession composition over exactly two fresh foreign Arena artifacts created after the AGENT_09 cut `2026-09-05T23:14:12.649Z`:

1. AGENT_27 O3R1 Proof-Neutral Rebind Attestation Hardening — provider-bound parent/child/generator/changed-path/allowlist observation; bare rebind booleans force REPROVE.
2. AGENT_08 O2R Workload-Qualified Efficiency Proof Reuse V2 — trace/resource and workload/cost projections are independently recomputed from raw current evidence; projection drift forces REPROVE.

Keeper:

`ReusableAcrossGeneratedChild => ProviderBoundProofNeutralMovement ∧ SameSemanticSource ∧ ExactProofTimeProjection ∧ ExactCurrentProjection ∧ NoRuntime/Trace/Cost/Benchmark/HardwareDrift ∧ D0`.

The current carrier child may differ from the proved parent only through a provider-bound proof-neutral generated movement. The efficiency projection deliberately remains anchored to the proved semantic source. A provider rebind cannot pay projection drift, and an exact projection cannot pay an unproven movement.

`provider_observation_verified` is an explicitly external prerequisite; this cell recomputes the observation root and all internal bindings but does not mint cryptographic/provider truth. Fresh hosted PASS remains false. No physical model performance, merge/deploy authority, effect authority, private/native transformer KV access, or Gate10.
