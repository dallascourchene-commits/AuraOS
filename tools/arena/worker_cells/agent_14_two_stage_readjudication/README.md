# AGENT_14 — Two-Stage Readjudication Compiler

D0-only composition of exactly two fresh foreign parents after AGENT_13:
1. Semantic-Domain-Bound Evidence Reproof O4 — local generation/projection/domain drift becomes a minimum dependency-closed local reproof cone.
2. O7 External Authentication Cutset — once local replay is exact, compute the minimum external attestation bundle still missing.

Ordering law: `LocalSemanticExact -> ExternalAuthenticationComplete -> ELIGIBLE_FOR_FRESH_READJUDICATION`.

Missing local proof cannot be paid by provider attestations. Missing external authentication cannot be paid by locally exact roots. Eligibility returns to the semantic owner and is not truth/effect/Gate10 authority.
