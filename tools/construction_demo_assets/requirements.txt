# Build-time only. Do not import these packages during Aura startup or demo runtime.
# Versions are initial compatibility pins and must be reverified by the deterministic asset build.
ifcopenshell==0.8.3.post2
numpy==2.3.1
trimesh==4.7.1
