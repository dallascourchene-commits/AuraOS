name: Model Connectome Bridge

on:
  pull_request:
    paths:
      - "aura_capability_connectome_v2.py"
      - "aura_capability_resolver_v2.py"
      - "aura_model_connectome_bridge.py"
      - "tests/test_aura_capability_connectome_v2.py"
      - "tests/test_aura_capability_resolver_connectome.py"
      - "tests/test_aura_model_connectome_bridge.py"
      - ".github/workflows/model-connectome-bridge.yml"
  push:
    branches:
      - main
      - refactor/model-connectome-bridge
    paths:
      - "aura_capability_connectome_v2.py"
      - "aura_capability_resolver_v2.py"
      - "aura_model_connectome_bridge.py"
      - "tests/test_aura_capability_connectome_v2.py"
      - "tests/test_aura_capability_resolver_connectome.py"
      - "tests/test_aura_model_connectome_bridge.py"
      - ".github/workflows/model-connectome-bridge.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  contracts:
    name: Connectome bridge contracts (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    timeout-minutes: 25
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.12"]
    steps:
      - name: Clone exact source branch
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SOURCE_REF: ${{ github.head_ref || github.ref_name }}
        run: |
          set -euo pipefail
          find . -mindepth 1 -maxdepth 1 -exec rm -rf {} +
          git clone --depth 1 --branch "${SOURCE_REF}" \
            "https://x-access-token:${GH_TOKEN}@github.com/${GITHUB_REPOSITORY}.git" .
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install test tooling
        run: python -m pip install --upgrade pytest ruff
      - name: Compile bridge modules
        run: |
          python -m py_compile \
            aura_capability_connectome_v2.py \
            aura_capability_resolver_v2.py \
            aura_model_connectome_bridge.py
      - name: Fatal lint checks
        run: |
          ruff check --select E9,F63,F7,F82 \
            aura_capability_connectome_v2.py \
            aura_capability_resolver_v2.py \
            aura_model_connectome_bridge.py \
            tests/test_aura_capability_connectome_v2.py \
            tests/test_aura_capability_resolver_connectome.py \
            tests/test_aura_model_connectome_bridge.py
      - name: Run bridge contracts
        run: |
          python -m pytest -q \
            tests/test_aura_capability_connectome.py \
            tests/test_aura_capability_connectome_v2.py \
            tests/test_aura_capability_resolver_connectome.py \
            tests/test_aura_model_connectome_bridge.py
