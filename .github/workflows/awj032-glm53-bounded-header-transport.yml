name: AWJ032 GLM53 Bounded Header Transport

on:
  # Branch push is intentionally scoped to this W2 source cone; CODEMAP-only churn does not match paths.
  push:
    branches:
      - "awj032/glm53-bounded-header-transport-20260830"
    paths:
      - "tools/awj032/glm53_safetensors_header_transport.py"
      - "tools/awj032/test_glm53_safetensors_header_transport.py"
      - ".github/workflows/awj032-glm53-bounded-header-transport.yml"
  pull_request:
    paths:
      - "tools/awj032/glm53_safetensors_header_transport.py"
      - "tools/awj032/test_glm53_safetensors_header_transport.py"
      - ".github/workflows/awj032-glm53-bounded-header-transport.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: awj032-glm53-header-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  bounded-header-contract:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    env:
      PR340_BRANCH: awj032/glm53-checkpoint-layout-probe-20260830
      REQUIRED_MTP_SHA: 746fe756d4af4bf78a2fc75e612a445e66e38e49
    steps:
      - name: Fetch exact head without persisted credentials
        env:
          GH_TOKEN: ${{ github.token }}
          TARGET_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        run: |
          git init .
          git remote add origin "https://github.com/${{ github.repository }}.git"
          AUTH_HEADER="$(printf 'x-access-token:%s' "$GH_TOKEN" | base64 -w0)"
          git -c http.https://github.com/.extraheader="AUTHORIZATION: basic $AUTH_HEADER" \
            fetch --no-tags --depth=1 origin "$TARGET_SHA"
          unset AUTH_HEADER
          git checkout --detach FETCH_HEAD
          test "$(git rev-parse HEAD)" = "$TARGET_SHA"
          test -z "$(git config --local --get-all http.https://github.com/.extraheader || true)"
      - name: Bind current PR340 MTP generation
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          AUTH_HEADER="$(printf 'x-access-token:%s' "$GH_TOKEN" | base64 -w0)"
          git -c http.https://github.com/.extraheader="AUTHORIZATION: basic $AUTH_HEADER" \
            fetch --no-tags --depth=64 origin \
            "refs/heads/$PR340_BRANCH:refs/remotes/origin/$PR340_BRANCH"
          unset AUTH_HEADER
          test -z "$(git config --local --get-all http.https://github.com/.extraheader || true)"
          CONSUMER_BASE_SHA="$(git rev-parse "refs/remotes/origin/$PR340_BRANCH")"
          git cat-file -e "$REQUIRED_MTP_SHA^{commit}"
          git merge-base --is-ancestor "$REQUIRED_MTP_SHA" "$CONSUMER_BASE_SHA"
          echo "CONSUMER_BASE_SHA=$CONSUMER_BASE_SHA" >> "$GITHUB_ENV"
          echo "PR340_CURRENTNESS_RECEIPT=$CONSUMER_BASE_SHA contains $REQUIRED_MTP_SHA"
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: "3.12"
      - name: Install focused test dependency
        run: python -m pip install "pytest>=8.2,<9.0"
      - name: Run bounded transport contract
        run: |
          python -m py_compile tools/awj032/glm53_safetensors_header_transport.py
          python -m pytest -q tools/awj032/test_glm53_safetensors_header_transport.py
      - name: Observe official GLM-5.3 header evidence without payload
        env:
          GLM53_REPO: zai-org/GLM-5.3
          GLM53_REVISION: 7cda81930d6e4cef42f48555de830aa32ecdde28
          GLM53_INDEX_SHA256: e0fe7f28c1f853d4824e4d796374e3dacf1fe470988773952c79b063768134bf
        run: |
          python - <<'PY'
          from dataclasses import asdict
          import json
          import os
          from tools.awj032.glm53_safetensors_header_transport import (
              HeaderTransportError,
              collect_header_evidence,
          )

          target = {
              "repo_id": os.environ["GLM53_REPO"],
              "model_revision": os.environ["GLM53_REVISION"],
              "expected_index_sha256": os.environ["GLM53_INDEX_SHA256"],
              "selected_layer": 3,
              "selected_expert": 0,
          }
          common = {
              "consumer_pr340_generation": os.environ["CONSUMER_BASE_SHA"],
              "required_mtp_repair_generation": os.environ["REQUIRED_MTP_SHA"],
              "repo_id": target["repo_id"],
              "model_revision": target["model_revision"],
              "index_sha256": target["expected_index_sha256"],
              "selected_layer": 3,
              "selected_expert": 0,
              "payload_bytes_read": 0,
              "g2_admitted": False,
              "runtime_executed": False,
              "authority": False,
          }
          try:
              receipt = collect_header_evidence(**target)
          except HeaderTransportError as exc:
              print("AURA_W2_OFFICIAL_HEADER_RESULT=" + json.dumps({
                  **common,
                  "status": "BLOCKED_TRANSPORT_TYPED",
                  "error_code": exc.code,
                  "detail": exc.detail,
              }, sort_keys=True, separators=(",", ":")))
              raise SystemExit(4)
          else:
              body = asdict(receipt)
              body.update({
                  "status": "OFFICIAL_HEADER_OBSERVED",
                  "consumer_pr340_generation": os.environ["CONSUMER_BASE_SHA"],
                  "required_mtp_repair_generation": os.environ["REQUIRED_MTP_SHA"],
                  "receipt_digest": receipt.receipt_digest,
              })
              assert body["payload_bytes_read"] == 0
              assert body["g2_admitted"] is False
              assert body["runtime_executed"] is False
              assert body["authority"] is False
              assert len(body["entries"]) == 6
              print("AURA_W2_OFFICIAL_HEADER_RESULT=" + json.dumps(
                  body, sort_keys=True, separators=(",", ":")
              ))
          PY
