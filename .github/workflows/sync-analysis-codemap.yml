name: Synchronize CODEMAP topology and source anchors — STACK SAFETY HOLD

# This nonpromoting Different-J child is stacked on PR350, whose base predates
# the credential-safe/head-stable CODEMAP V2 owner. Automated CODEMAP sync is
# disabled on this child. Before integration, rebase the canonical owner lane
# onto PR393 (or its landed successor) rather than merging this hold as policy.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  safety-hold:
    runs-on: ubuntu-24.04
    steps:
      - run: |
          echo "STACK_SAFETY_HOLD: automated CODEMAP mutation disabled"
          echo "Rebase canonical owner onto hardened CODEMAP V2 before integration."
