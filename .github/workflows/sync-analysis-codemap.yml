name: Synchronize CODEMAP topology and source anchors

# STACK-LOCAL SAFETY HOLD for nonpromoting AWJ032 PR340 producer-snapshot child.
# The parent PR340 generation predates the credential-safe/head-stable CODEMAP V2
# lane. Do not auto-run a branch-pushing diagnostic workflow on this child.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  safety-hold:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'CODEMAP auto-mutation disabled on this nonpromoting stacked child; rebase hardened workflow before integration.'
