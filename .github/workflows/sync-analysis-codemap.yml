name: Synchronize CODEMAP on architecture work branches

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  temporarily-disabled-for-pr240-repair:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "PR240 repair staging guard"
