name: P3.1 Planning event projection

on:
  pull_request:

jobs:
  carrier-trigger:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Carrier trigger only; base workflow publishes exact C6-C9 patch."
