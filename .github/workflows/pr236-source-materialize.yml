name: PR236 repository identity repair

on:
  pull_request:
    types: [synchronize]

permissions:
  contents: write

jobs:
  repair:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout exact branch head
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          ref: refactor/bilateral-intent-pr3-enforcement

      - name: Install repository test requirements
        run: python -m pip install --disable-pip-version-check -r requirements.txt

      - name: Apply repository identity repair
        run: python .github/pr236_identity_fix.py.tmp

      - name: Verify focused bilateral behavior
        shell: bash
        run: |
          set -euo pipefail
          python -m pytest -q tests/test_aura_bilateral_planning_enforcement.py
          python -m ruff check \
            aura_arena_architect_connector.py \
            aura_architect_loop.py \
            tests/test_aura_bilateral_planning_enforcement.py \
            --select E9,F63,F7,F82
          python -m py_compile \
            aura_arena_architect_connector.py \
            aura_architect_loop.py \
            tests/test_aura_bilateral_planning_enforcement.py

      - name: Commit verified repair and remove temporary executors
        shell: bash
        run: |
          set -euo pipefail
          rm -f .github/workflows/pr236-source-materialize.yml
          rm -f .github/pr236_manual_repairs.py
          rm -f .github/pr236_identity_fix.py.tmp
          rm -f .github/pr236_identity_fix_workflow.yml.tmp
          rm -f .aura/pr236_source_materialization.trigger
          git config user.name "dallascourchene-commits"
          git config user.email "281686929+dallascourchene-commits@users.noreply.github.com"
          git add -A -- \
            aura_arena_architect_connector.py \
            aura_architect_loop.py \
            tests/test_aura_bilateral_planning_enforcement.py \
            .github/workflows/pr236-source-materialize.yml \
            .github/pr236_manual_repairs.py \
            .github/pr236_identity_fix.py.tmp \
            .github/pr236_identity_fix_workflow.yml.tmp \
            .aura/pr236_source_materialization.trigger
          git commit -m "fix(pr236): fail closed on repository identity errors"
          git push origin HEAD:refactor/bilateral-intent-pr3-enforcement
