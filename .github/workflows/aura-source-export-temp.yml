name: Aura Temporary Source Export

on:
  pull_request:
    paths:
      - ".github/workflows/aura-source-export-temp.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  export-source:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out exact pull-request head
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 1
          submodules: false

      - name: Verify exact head and archive source
        env:
          EXPECTED_HEAD: ${{ github.event.pull_request.head.sha || github.sha }}
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$EXPECTED_HEAD"
          echo "TREE_SHA=$(git rev-parse 'HEAD^{tree}')"
          tar --exclude=.git -czf /tmp/aura-source-export.tar.gz .

      - name: Upload source archive
        uses: actions/upload-artifact@v4
        with:
          name: aura-source-export-${{ github.event.pull_request.head.sha || github.sha }}
          path: /tmp/aura-source-export.tar.gz
          if-no-files-found: error
          retention-days: 1
