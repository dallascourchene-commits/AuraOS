name: C3-C5 PR Publisher

on:
  pull_request:
    types: [synchronize]

permissions:
  contents: write
  pull-requests: read

jobs:
  publish:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporarily disabled while chunked payload is staged."
