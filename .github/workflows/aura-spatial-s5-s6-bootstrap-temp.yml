name: Aura Temporary S5-S6 Materialization Trigger

on:
  pull_request:
    paths:
      - ".github/workflows/aura-spatial-s5-s6-bootstrap-temp.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  trigger-materializer:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - run: echo "Triggering corrected exact Spatial S5/S6 finalization."
