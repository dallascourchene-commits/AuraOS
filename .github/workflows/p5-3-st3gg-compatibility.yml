name: P5.3 ST3GG compatibility facades

on:
  pull_request:
  push:
    branches:
      - main
      - "refactor/**"

permissions:
  contents: read

concurrency:
  group: p5-3-st3gg-compatibility-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  focused-st3gg-compatibility:
    name: ST3GG compatibility (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    timeout-minutes: 25
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - name: Install focused tooling
        run: |
          python -m pip install --upgrade pip
          python -m pip install numpy pytest ruff
      - name: Compile legacy, canonical, and compatibility surfaces
        run: |
          python -m py_compile \
            aura_st3gg_codec.py \
            aura_st3gg_recall.py \
            aura_arena_st3gg_codec.py \
            aura_arena_st3gg_egress.py \
            aura_st3gg_contracts.py \
            aura_arena_st3gg_shadow.py \
            aura_st3gg_compatibility.py \
            aura_st3gg_compatibility_types.py \
            aura_st3gg_compatibility_recall.py \
            test_aura_arena_st3gg_codec.py \
            tests/test_aura_st3gg_codec.py \
            tests/test_aura_st3gg_contracts.py \
            tests/test_aura_arena_st3gg_shadow.py \
            tests/test_aura_arena_st3gg_shadow_hardening.py \
            tests/test_aura_st3gg_compatibility.py
      - name: Fatal and static correctness lint
        run: |
          ruff check --select E4,E7,E9,F \
            aura_st3gg_compatibility.py \
            aura_st3gg_compatibility_types.py \
            aura_st3gg_compatibility_recall.py \
            tests/test_aura_st3gg_compatibility.py
      - name: Run focused compatibility matrix
        id: focused-tests
        shell: bash
        run: |
          set +e
          python -m pytest -q \
            tests/test_aura_st3gg_compatibility.py \
            tests/test_aura_st3gg_contracts.py \
            tests/test_aura_st3gg_codec.py \
            tests/test_aura_arena_st3gg_shadow.py \
            tests/test_aura_arena_st3gg_shadow_hardening.py \
            test_aura_arena_st3gg_codec.py \
            > "p5-3-pytest-${{ matrix.python-version }}.txt" 2>&1
          status=$?
          echo "status=$status" >> "$GITHUB_OUTPUT"
          cat "p5-3-pytest-${{ matrix.python-version }}.txt"
          exit 0
      - name: Upload focused diagnostics on failure
        if: steps.focused-tests.outputs.status != '0'
        uses: actions/upload-artifact@v4
        with:
          name: p5-3-pytest-${{ matrix.python-version }}
          path: p5-3-pytest-${{ matrix.python-version }}.txt
          if-no-files-found: error
          retention-days: 7
      - name: Enforce focused test result
        if: steps.focused-tests.outputs.status != '0'
        run: exit 1
