             tests/test_aura_coding_waboose.py \
             tests/test_aura_waboose_learning.py
diff --git a/.github/workflows/aura-spatial-s3a-materialize.yml b/.github/workflows/aura-spatial-s3a-materialize.yml
deleted file mode 100644
index 74888e05ad4638e82a38ac5996e480932e97edd6..0000000000000000000000000000000000000000
--- a/.github/workflows/aura-spatial-s3a-materialize.yml
+++ /dev/null
@@ -1,52 +0,0 @@
-name: Aura Spatial S3A Materialize
-
-on:
-  pull_request:
-    paths:
-      - ".github/workflows/aura-spatial-s3a-materialize.yml"
-
-permissions:
-  contents: read
-
-concurrency:
-  group: aura-spatial-s3a-materialize-${{ github.event.pull_request.number }}
-  cancel-in-progress: true
-
-jobs:
-  export-exact-head:
-    runs-on: ubuntu-latest
-    timeout-minutes: 10
-    steps:
-      - name: Fetch and verify exact pull-request head archive
-        shell: bash
-        env:
-          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
-          EXACT_HEAD: ${{ github.event.pull_request.head.sha }}
-        run: |
-          set -euo pipefail
-          curl --fail --location --retry 3 --retry-all-errors \
-            -H "Accept: application/vnd.github+json" \
-            -H "Authorization: Bearer ${GH_TOKEN}" \
-            -H "X-GitHub-Api-Version: 2022-11-28" \
-            -o exact-head.tar.gz \
-            "https://api.github.com/repos/${GITHUB_REPOSITORY}/tarball/${EXACT_HEAD}"
-          size="$(wc -c < exact-head.tar.gz)"
-          test "$size" -gt 0
-          test "$size" -le 268435456
-          mkdir -p verify
-          tar -xzf exact-head.tar.gz -C verify --strip-components=1
-          test -f verify/aura_spatial_contracts.py
-          test -f verify/aura_source_integrity.py
-          printf '%s\n' "$EXACT_HEAD" > EXACT_HEAD.txt
-
-      - name: Upload exact-head repository archive
-        uses: actions/upload-artifact@v4
-        with:
-          name: aura-spatial-s3a-exact-head-${{ github.event.pull_request.head.sha }}
-          path: |
-            exact-head.tar.gz
-            EXACT_HEAD.txt
-          if-no-files-found: error
-          retention-days: 1
-
-# Temporary refresh marker: bind local implementation to the post-P0 exact head.
diff --git a/aura_node.py b/aura_node.py
index 3dce9a585813297dc3db3d793d57ed178a9bfd58..d57041757d08830292e0870ef8084a2dda29163f 100644
--- a/aura_node.py
+++ b/aura_node.py
@@ -224,6 +224,7 @@ from aura_gbnf_profiles import (
     grammar_stop_tokens,
 )
 from aura_pre_egress_interceptor import apply_pre_egress_profile
+from aura_source_integrity import SourceIntegrityError, read_utf8_source
 from aura_spvm import get_semantic_vector as _spvm_get_semantic_vector
 from llama_server_manager import LlamaServerManager
 from symbolic_shield import verify_structural_truth
@@ -2127,17 +2128,48 @@ class AuraEcosystemAuditor:
     def __init__(self, node_ref):
         self.node = node_ref
         self.function_calls = defaultdict(int)
-    async def _scan_and_stamp_file(self, file_path: str, q_root: str):
+        self.audit_failures: list[dict[str, Any]] = []
+
+    async def _scan_and_stamp_file(self, file_path: str, q_root: str) -> bool:
         if not os.path.exists(file_path):
-            return
-        with open(file_path, encoding='utf-8') as f:
-            source_code = f.read()
+            return False
+        try:
+            source_code = read_utf8_source(file_path)
+        except SourceIntegrityError as exc:
+            failure = exc.failure.to_dict()
+            failure["path"] = Path(file_path).as_posix()
+            self.audit_failures.append(failure)
+            print(f"[-] Source integrity failure in {file_path}: {exc}")
+            return False
+        except OSError as exc:
+            self.audit_failures.append(
+                {
+                    "path": Path(file_path).as_posix(),
+                    "code": "SOURCE_FILESYSTEM_ERROR",
+                    "message": str(exc),
+                    "byte_offset": 0,
+                    "offending_bytes_hex": "",
+                    "file_size": 0,
+                }
+            )
+            print(f"[-] Unable to read {file_path} during audit: {exc}")
