# Arena Capsule Bindings

These versioned overlays bind existing Arena transitions to repository-local morphology profiles and Executable Route Capsules without embedding code in grammar manifests.

Overlays are validated against the exact `arena_id`, `grammar_version`, and transition IDs. They do not grant runtime, patch, lease, promotion, commit, push, or merge authority.
