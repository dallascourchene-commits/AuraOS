{
  "name": "Aura Agent Arena",
  "image": "mcr.microsoft.com/devcontainers/python:3.12",
  "postCreateCommand": "python -m pip install -U pip && python -m pip install -r requirements.txt || true && chmod +x scripts/aura-agent-arena && python scripts/aura-agent-arena digest",
  "remoteEnv": {
    "AURA_AGENT_ARENA_MODE": "codespaces",
    "AURA_AGENT_ARENA_PATCH_AUTHORITY": "exact_source_spans_and_hashes_only"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "github.copilot"
      ]
    }
  }
}