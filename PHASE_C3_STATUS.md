# Phase C3 Status

Status: `DRAFT / REVIEW REQUIRED`

This branch starts fresh from the merged and CodeRabbit-repaired Phase C2 baseline. It adds isolated, explicitly leased route-capsule trials and proposal-only Agent IR procedure induction.

It does **not** activate a winning capsule, install generated code, mutate the live grammar, commit, push, open or merge a pull request automatically.

Current implementation scope:

- baseline plus single-axis tightening variants over seven declared proposal-safe limits;
- built-in-only trial execution through Aura's existing ephemeral sandbox contract;
- strict TRAIN-only selection with independent VALIDATION and SHADOW assessment;
- deterministic output, usage, OutcomeVector, budget and dissolution measurements;
- procedure induction through `TEXT -> TYPED -> SPEC -> STUB -> SHIM -> PURE`;
- separate SQLite WAL trial ledger;
- review-only CLI and focused tests.

CODEMAP regeneration and final repository-native CI remain required before merge.
