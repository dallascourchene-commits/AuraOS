# Base demo and CI use the Python standard library only.
# Install this file in the AMD notebook when LoRA crystallization is enabled.
transformers>=4.51,<5
datasets>=3.0,<5
peft>=0.15,<1
trl>=0.17,<1
accelerate>=1.2,<2
# Install the ROCm-compatible torch build supplied by the AMD notebook image.
