# Objective O1

**Objective:** absorb the September 2026 AirLLM streamed-LoRA training source change without widening AWJ032 authority, determine which GLM/Qwen families are actually implemented, and add a source-bound adapter admission contract that cannot silently accept partial state or an unbound base/runtime.

**Outcome:** achieved D0/nonpromoting.

- Qwen3.5/Qwen3.8 dense route: source-family admitted.
- Qwen4Exp/Flash-Next route: source-family admitted.
- GLM/GLM-MoE-DSA streamed training: `HOLD_PORT_REQUIRED`.
- exact adapter key coverage required.
- base/config/tokenizer/runtime/AirLLM-source/target/value provenance required.
- no model training or tensor-payload access performed.

The next numbered objective requires two fresh foreign consequence-distinct Aura artifacts or a typed physical/owner-host wake; projections of this O1 do not qualify.
