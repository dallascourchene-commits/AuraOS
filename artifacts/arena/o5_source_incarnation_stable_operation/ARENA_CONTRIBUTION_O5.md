# O5 — Source-Incarnation Stable Operation Contract

State: ACHIEVED_LOCAL_D0_NONPROMOTING; hosted exact-head credit pending.

## Rebase

Parent A: Project006 O17 independent source incarnation (`15c9...`): shared transport digest is not independently recomputed source identity.

Parent B: O-AVX11 canonical creative handoff (`1Ewh...`): navigation/agent/workcell rebinding preserves semantic operation identity while rotating concrete attempt identity.

## New law

`IndependentSourceIncarnation + SemanticOccurrence => StableOperation`; `CurrentCanonicalAdmission => ConcreteAttempt`.

The stable root binds full source incarnation (`file_id`, `revision_id`, export MIME, independently recomputed export root), domain, operation kind, intended occurrence, semantic payload, and policy. It excludes actor, card, K27 coordinate, hydration, lease, host/admission generation, and currentness. Those volatile coordinates are attempt-only.

The module consumes O17-shaped source objects and independently recomputes the O17 incarnation root. It does not create a second transport source owner and never infers an unknown transport digest normalization.

## Proof

3 recreated stdlib venvs; 48/48 focused executions PASS. Deterministic campaign output SHA-256 `3c6373a5ae5c8b871511d80c5af3249c91f3697cca26d457ff1beadd1b429024` in all three. Campaign 24,000 cases/env with 0 oracle mismatches, false READY, or false HOLD; 6,000 source differentials with 0 semantic failures-to-rotate; naive same-content digest collapses 2,000 same-byte/different-incarnation cases. Ω8: 6,561 states, one keeper, zero invalid READY. Factored 13D: 6,561 real hard executions + all 243 nuisance contexts on sole keeper + hashed Cartesian 1,594,323 projection; zero invalid contextual repair. HS1000: 1,000 frozen cells -> 16 consequence quotients; claimed breakthroughs=0.

## Authority ceiling

D0 only. No provider/effect/training/checkpoint/transport-digest/merge/deploy/Gate10 authority. K27 coordinates are retrieval locality only.
