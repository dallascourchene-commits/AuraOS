# AWJ032 AirLLM / Qwen O2 — proof-carrying adapter transitions

Status: D0 / nonpromoting. No model execution, deployment, provider spend, checkpoint mutation, merge, or Gate10.

## Historical O2 parents and objective

O2 originally rebased two consequence-distinct foreign parents: the O-AVX9 boundary-safe transition artifact and the Post-O16 physical transport/deployment-gap receipt. Their surviving law remains useful: reusable cores stay immutable while boundary semantics live in separate content-addressed nodes, and physical provider success does not itself prove deployment of those semantics.

The transition envelope binds adapter/base transition identity, inference runtime, cache policy, deployment generation and activation mode. O4 strengthens this by carrying the signed O1 manifest preimage through R1 into O2 and requiring the `AdapterCore` source, topology, base/config/tokenizer/value preimage and family/trainer to match that inherited permit before physical trace evidence can admit the transition.

## Current proof status

The earlier O2 artifact recorded **21 tests per environment and 10,000 tamper cases per environment**, but the present tracked tree does not contain a harness that reconstructs those exact historical counts. Therefore those numbers now have **zero current-head proof credit** and are retained only as historical provenance in `PROOF_RECEIPT_O2.json`.

Current reproducible evidence is the O4 post-review proof:

- current tracked O2 test methods: **10**;
- current dedicated tracked O2 10k tamper harness: **none** — no such count is claimed;
- cross-cone O4: **45 tests × 3 published-identity fresh environments = 135/135 PASS**, with one explicit pinned-upstream-fixture skip per environment in O1 only;
- O4 adversarial search: **20,000 frozen cells → 8 consequence classes**, enumeration credit = 0;
- O4 proof path: `artifacts/arena/awj032_airllm_training_o4/PROOF_RECEIPT_O4_PUBLISHED.json`;
- O4 receipt root: `d4f5112e9bea6eef7d2bdc235c403d600c64e20c9a0c2b1801936e9fcb6b552e`.

This correction is intentional: unreconstructible historical volume cannot substitute for current tracked evidence.

## Current keeper laws

- authenticated physical ACK/RESULT != permission unless transition identity and current signed permit also match;
- shared source digest != independently recomputed source identity;
- AdapterCore opaque identity != admitted preimage unless base/config/tokenizer/value/family/source/topology are inherited and checked;
- changed roots reopen only referencing transitions;
- GLM-5.3 streamed training remains `HOLD_PORT_REQUIRED`.

## External pressure

FASTLIBRA, Activated LoRA serving, LRAgent and Scale-QLoRA remain methodology pressure around adapter/KV/deployment dependency boundaries. O4 additionally draws design pressure from durable authorization and capability-lease work, but external research never creates Aura authority.
