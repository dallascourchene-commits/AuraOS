# AWJ032 AirLLM / Qwen O2 — proof-carrying adapter transitions

Status: D0 / nonpromoting. No model execution, deployment, provider spend, checkpoint mutation, merge, or Gate10.

## Two fresh foreign parents

1. `ARENA-CONTRIBUTION__O-AVX9__BOUNDARY-SAFE-TRANSITIONS__75PASS__GPT56SOL__20260907` — Drive `1Y4R5iX5yU2G4-AcXvfVDE9ohYnk1QEwM`. Consequence imported: keep reusable cores immutable and represent boundary semantics as separate content-addressed nodes so only adjacent/reference cones reopen.
2. `EMPIRICAL-RECEIPT__POST-O16__R3-PHYSICAL-TRANSPORT-LINEAGE-AND-DEPLOYMENT-GAP__GPT56SOL__20260907` — Drive `18ZsY5e2KLzQLBr8ConI1qp7PxL-giOFRzashya6-nvM`. Consequence imported: physical provider success does not prove deployment of proof-carrying semantics; a shared source digest does not substitute for independent source-identity recomputation.

Both were created after O1's cut and are consequence-distinct.

## Objective

Bind the O1 training-admission identity into the adapter training→inference boundary without mutating reusable base/adapter cores. A `TransitionEnvelope` binds the exact O1 receipt root, base core, previous/next adapter core, inference runtime, cache policy, deployment generation, and activation mode. Runtime adapter activation and merged-checkpoint deployment remain distinct modes; merged deployment additionally requires an exact merge-grid root.

A physical ACK/RESULT earns `ADMIT_D0_PROOF_CARRYING_TRANSITION` only when it binds the exact transition root, training-admission root, adapter-core root, runtime root, command/attempt/idempotency tuple, temporal order, and an independently recomputed source-envelope digest. Provider success without those fields remains `HOLD_UNBOUND_PHYSICAL_SUCCESS`.

## Selective reproof

A changed adapter/base/runtime/cache/training/merge-grid root reopens only transition envelopes that reference that root. Unrelated adapter transitions remain reusable.

## Proof

- 21 focused tests in each of 3 deleted/recreated stdlib virtual environments = 63/63 PASS.
- 10,000 transition-identity tamper cases per environment; zero admitted mutants.
- Ω8: all 3^8 = 6,561 states; one keeper.
- factored 13D: all 3^13 = 1,594,323 states; one keeper; zero hard-invalid contextual repairs.
- HS1000: exactly 1,000 frozen cells quotient to six consequence classes; enumeration earns zero breakthrough credit.
- receipt root: `5807524f16aa7a65e07ceb0af1dbebf2fada73d83dafcc17ea91234d0b7a919e`.
- proof JSON SHA-256: `109e830a8d1abfee792cf60370a556ef9b6f2f905a5d5541172c4abd5cd962d4`; byte-identical across all three venvs.

## External pressure

FASTLIBRA (arXiv:2505.03756) treats LoRA and KV cache objects as dependency-coupled serving state. Activated LoRA serving (arXiv:2512.17910) makes adapter activation boundaries explicit enough for base-aligned prefix-cache reuse. LRAgent (arXiv:2602.01053) separates shared-base and adapter-dependent KV contributions. Scale-QLoRA (arXiv:2609.04526) shows that merged deployment identity can depend on the exact native quantization/grid lifecycle.

These sources are design pressure only and grant no Aura authority.

## Current model-family ceiling

The source-bound O1 law is unchanged: the pinned AirLLM generation explicitly implements Qwen streamed training routes; GLM-5.3 streamed training remains `HOLD_PORT_REQUIRED` until a GLM-specific trainer/port is independently present and re-proved.
