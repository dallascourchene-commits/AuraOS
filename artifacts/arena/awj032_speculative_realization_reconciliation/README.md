# AWJ032 O5 — Speculative Realization Reconciliation

O5 begins **after** speculative materialization. It does not decide what to prefetch or stage.

Keeper: `SpeculativeBytesPresent != CanonicalAdoption != EffectTimeUse`.

- Same bytes from a losing candidate dissolve; byte identity cannot override the canonical winner.
- GLM expert/FP8 slice bytes are reusable only when O20's exact static ABI/native-route/slice bindings remain current, and training activation remains `HOLD_EMPIRICAL_PORT_RUN` until the owner-host smoke exists.
- Qwen adapter bytes may be retained as immutable D0 cache material when the exact candidate/currentness bindings still match. Serving activation additionally requires exact O4 effect-time realization/cache/process evidence.
- No reconciliation decision mints effect, training, checkpoint, merge, Gate10, provider, or owner-host authority.

The Front Door Continuous Orientation V0.3 runtime was used to compile the actual Arena tool neighborhood and CRUISE/CUSTOMS obligations for this work. `FRONTDOOR_RUNTIME_BINDING_LOCAL.json` binds the exact donor/runtime roots used by the local three-venv proof; it is not a claim that AuraOS is installed/current on the owner's laptop.
